import AuthCard from "../../components/auth-card/AuthCard";
import "./style.css";

const Auth = () => {
  return (
    <div className="auth-container">
      <AuthCard />
    </div>
  );
};

export default Auth;
